require('nvim-tree').setup {
    renderer = {
        icons = {
            git_placement = "after",
            show = {
                file = false,
                folder = false,
                folder_arrow = false,
            }
        }
    }
}
